const config = [
        {
            name: "HailuoMCPServer",
            type: "command",
            command: "node ./build/server.js",
            isOpen: true
        }
    ];
export default config;